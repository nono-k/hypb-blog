---
const { url } = Astro.props;
---

<a class="demo-btn" href={url} target="_blank">デモを見る</a>

<style lang="scss">
  .demo-btn {
    display: block;
    width: fit-content;
    margin-left: auto;
    padding: 0.5rem 1.5rem;
    border-radius: 0.5rem;
    color: var(--black);
    font-weight: 500;
    background: var(--yellow);
    text-decoration: none;
    transition: background-color 0.3s;
    @media (hover: hover) {
      &:hover {
        background-color: var(--black);
        color: var(--gray-10);
      }
    }
  }
</style>